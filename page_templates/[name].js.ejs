const a= "我是模板";
import a<%- pageName %> from b<%- pageName %>
const fs = require('fs');
const path = require('path');
const <%- pageName %> = require('<%- pageName %>')

// 输出文件配置项 
const config = {
    outputPath: '',
    outputName: ''
}
